idf_component_register(
    SRCS
        "mqtt_reporter.c"
    INCLUDE_DIRS
        "."
    REQUIRES
        mqtt
        esp_event
        esp_system
        freertos
        nvs_flash
    EMBED_TXTFILES
        "certs/mqtt_broker_ca.pem"
)
